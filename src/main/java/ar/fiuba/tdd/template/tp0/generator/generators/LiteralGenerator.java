package ar.fiuba.tdd.template.tp0.generator.generators;

import ar.fiuba.tdd.template.tp0.tokenizer.Token;

public class LiteralGenerator implements StringGenerator {

    @Override
    public String generate(Token token) {
        return "";
    }

    private Integer getLength(Token token) {
//        Optional<Quantifier> quantifier = token.getQuantifier();

        return null;
//        if (quantifier.isPresent())
//            return null;
    }

}
